<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-header bg-primary text-white">
          <h3 class="mb-0">Login to TastyTrades UI</h3>
        </div>
        <div class="card-body">
          <% if flash[:alert] %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
              <%= flash[:alert] %>
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
          <% end %>

          <%= form_with url: login_path, local: true do |form| %>
            <div class="mb-3">
              <%= form.label :email, class: "form-label" %>
              <%= form.email_field :email, class: "form-control", required: true, autofocus: true %>
            </div>

            <div class="mb-3">
              <%= form.label :password, class: "form-label" %>
              <%= form.password_field :password, class: "form-control", required: true %>
            </div>

            <hr class="my-4">
            
            <h5 class="text-muted">TastyTrade API Connection (Optional)</h5>
            <p class="small text-muted">Connect to your TastyTrade account for live trading</p>
            
            <% if Rails.env.sandbox? %>
              <div class="alert alert-warning">
                <strong>🧪 Sandbox Mode Active</strong><br>
                <small>
                  • Use sandbox credentials from <a href="https://developer.tastytrade.com/" target="_blank">developer.tastytrade.com</a><br>
                  • These are NOT your live trading credentials<br>
                  • Connecting to: <%= ENV['TASTYTRADE_API_URL'] %>
                </small>
              </div>
            <% elsif Rails.env.production? %>
              <div class="alert alert-danger">
                <strong>⚠️ Live Trading Mode</strong><br>
                <small>
                  • Use your real TastyTrade credentials<br>
                  • Real money trades will be executed<br>
                  • Connecting to: <%= ENV['TASTYTRADE_API_URL'] %>
                </small>
              </div>
            <% else %>
              <div class="alert alert-info">
                <strong>🔧 Development Mode</strong><br>
                <small>
                  • Use sandbox or test credentials<br>
                  • Connecting to: <%= ENV['TASTYTRADE_API_URL'] %>
                </small>
              </div>
            <% end %>

            <div class="mb-3">
              <%= form.label :tastytrade_username, "TastyTrade Username/Email", class: "form-label" %>
              <%= form.text_field :tastytrade_username, class: "form-control", 
                  placeholder: Rails.env.sandbox? ? "sandbox-username" : "your.email@example.com" %>
              <div class="form-text">Your TastyTrade login email or username</div>
            </div>

            <div class="mb-3">
              <%= form.label :tastytrade_password, "TastyTrade Password", class: "form-label" %>
              <%= form.password_field :tastytrade_password, class: "form-control" %>
              <div class="form-text">Your TastyTrade account password</div>
            </div>

            <div class="d-grid">
              <%= form.submit "Login", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

          <div class="text-center mt-3">
            <p class="mb-0">Don't have an account? <%= link_to "Sign up", signup_path %></p>
          </div>
        </div>
      </div>

      <div class="alert alert-info mt-3">
        <h5 class="alert-heading">🛡️ Risk Management Active</h5>
        <p class="mb-0">This system enforces a 25% cash reserve requirement to protect your capital. You can never trade with your last 25% of available funds.</p>
      </div>
    </div>
  </div>
</div>